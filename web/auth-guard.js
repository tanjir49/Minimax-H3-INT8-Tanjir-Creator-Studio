(()=>{'use strict';const scrubSensitiveQuery=()=>{const params=new URLSearchParams(location.search);if(params.has('username')||params.has('password'))history.replaceState(null,'',location.pathname+location.hash)};scrubSensitiveQuery();const form=document.getElementById('auth-form');if(!form)return;form.method='post';form.action='/api/login';form.addEventListener('submit',async event=>{event.preventDefault();event.stopImmediatePropagation();const error=document.getElementById('auth-error');const button=form.querySelector('button[type="submit"]');if(error)error.textContent='';if(button)button.disabled=true;try{const setup=form.dataset.setup==='1';const username=document.getElementById('username')?.value||'';const password=document.getElementById('password')?.value||'';const remember=!setup&&Boolean(document.getElementById('remember-login')?.checked);const bootstrapToken=document.getElementById('bootstrap-token')?.value||'';const response=await fetch(setup?'/api/setup/admin':'/api/login',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({username,password,bootstrap_token:bootstrapToken,remember_me:remember})});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.error||data.message||'Sign in failed. Check your username and password.');if(!setup){localStorage.setItem('studio-remember-login',remember?'1':'0');if(remember)localStorage.setItem('studio-remembered-username',username.trim());else localStorage.removeItem('studio-remembered-username')}const passwordInput=document.getElementById('password');if(passwordInput)passwordInput.value='';location.replace('/') }catch(problem){if(error)error.textContent=problem.message||'Sign in failed.';if(button)button.disabled=false}},true)})();
